Create a new database table in Supabase. Ask the user what data they want to store.

Steps:
1. Determine the table name and columns based on the user's description
2. Write the SQL to create the table (use the Supabase dashboard SQL editor or MCP if connected)
3. Add Row Level Security (RLS) policies so users can only access their own data
4. Create a server action in the app to insert and query data from the new table
5. Show the user how to verify the table was created in the Supabase dashboard
